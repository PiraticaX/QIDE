# Quantum Enterprise Platform — Backend Dependencies
# Python 3.11 compatible (do NOT use Python 3.12+ due to breaking changes)

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database (async SQLAlchemy + SQLite for local, PostgreSQL for prod)
sqlalchemy[asyncio]==2.0.30
aiosqlite==0.20.0
asyncpg==0.29.0          # PostgreSQL async driver
alembic==1.13.1

# Settings
pydantic==2.7.1
pydantic-settings==2.2.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP
httpx==0.27.0

# Quantum (optional — graceful fallback if not installed)
# qiskit==1.1.0
# qiskit-aer==0.14.0

# Utils
python-multipart==0.0.9
email-validator==2.1.1

# Monitoring (optional)
# sentry-sdk[fastapi]==2.3.1
